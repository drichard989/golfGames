{
  "scripts": {
    "bump:cache": "node scripts/bump-cache-version.js",
    "hooks:install": "git config core.hooksPath .githooks",
    "prepare": "npm run --silent hooks:install || true"
  },
  "dependencies": {
    "firebase": "^12.12.1",
    "firebase-functions": "^4.9.0",
    "functions": "^1.0.9"
  }
}
